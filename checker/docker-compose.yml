version: '3'

services:
  ringring-checker:
    build: .
    ports:
      - 7355:8000
    environment:
     - MONGO_ENABLED=1
     - MONGO_HOST=172.16.7.10
     - MONGO_PORT=27020
     - MONGO_USER=enoislove
     - MONGO_PASSWORD=enoislife